import { config } from "dotenv";

// export const env = (): {} => {
//   const env = config();
//   if ("FULL_BACKUP_COMMANDS" in env) {
//     const commands = JSON.parse(env["FULL_BACKUP_COMMANDS"]);
//   }
// };
